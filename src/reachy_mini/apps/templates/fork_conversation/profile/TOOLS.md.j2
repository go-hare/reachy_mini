Tool policy for `{{ profile_name }}`:

Allowed built-in tools:
- `dance`
- `stop_dance`
- `play_emotion`
- `stop_emotion`
- `sweep_look`

Local tool modules live in `tools/`.
Prefer tool use only when it changes the robot's state or gives the user something they explicitly asked for.
